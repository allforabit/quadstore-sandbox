# Quadstore sandbox

Generate data:

```sh
yarn clear-and-generate
```

Run sparql query:

```
yarn run-query
```

(finishes in around 20 seconds)

Run get:

```
yarn run-get
```

(finishes in 165 ms)
