import { Literal, Term } from 'rdf-js';
import { ImportedPattern, ImportedQuad, ImportedRange, Pattern, Prefixes, Quad, Range } from '../types';
export declare const importLiteralTerm: (term: Literal, prefixes: Prefixes, rangeBoundary?: boolean) => string;
export declare const importSimpleTerm: (term: Term, isGraph: boolean, defaultGraphValue: string, prefixes: Prefixes, rangeBoundary?: boolean) => string;
export declare const importRange: (range: Range, prefixes: Prefixes, rangeBoundary?: boolean) => ImportedRange;
export declare const importTerm: (term: Term | Range, isGraph: boolean, defaultGraphValue: string, prefixes: Prefixes, rangeBoundary?: boolean) => string | ImportedRange;
export declare const importQuad: (quad: Quad, defaultGraphValue: string, prefixes: Prefixes) => ImportedQuad;
export declare const serializeImportedQuad: (quad: ImportedQuad) => string;
export declare const importPattern: (terms: Pattern, defaultGraph: string, prefixes: Prefixes) => ImportedPattern;
