/// <reference types="node" />
import { DataFactory } from 'rdf-js';
import { Pattern, Quad, TermName } from '../types';
export declare const copyBufferIntoBuffer: (source: Buffer, target: Buffer, targetFrom: number) => void;
export declare const writeQuad: (key: Buffer, keyOffset: number, value: Buffer, valueOffset: number, separator: Buffer, quad: Quad, termNames: TermName[]) => {
    key: Buffer;
    value: Buffer;
};
export declare const writePattern: (pattern: Pattern, gt: Buffer, gtFrom: number, lt: Buffer, ltFrom: number, separator: Buffer, boundary: Buffer, termNames: TermName[]) => false | {
    gt: Buffer;
    gte: boolean;
    lt: Buffer;
    lte: boolean;
};
export declare const readQuad: (key: Buffer, keyOffset: number, value: Buffer, valueOffset: number, separator: Buffer, termNames: TermName[], factory: DataFactory) => Quad;
