import { DataFactory, Literal } from 'rdf-js';
import { ImportedQuad, Prefixes, Quad } from '../types';
export declare const exportLiteralTerm: (term: string, dataFactory: DataFactory, prefixes: Prefixes) => Literal;
export declare const exportQuad: (quad: ImportedQuad, defaultGraphValue: string, dataFactory: DataFactory, prefixes: Prefixes) => Quad;
export declare const deserializeImportedQuad: (quad: string) => ImportedQuad;
